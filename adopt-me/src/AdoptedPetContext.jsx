import { createContext } from "react";

// we are use context for after select pet and then click on button adopt "yes" than pet image will be show on starting page.

const AdoptedPetContext = createContext();

export default AdoptedPetContext;
